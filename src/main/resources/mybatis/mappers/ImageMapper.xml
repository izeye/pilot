<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ctb.pilot.game.memory.dao.mybatis.ImageMapper">
	<resultMap id="image" type="GameImage">
		<id column="seq" property="sequence" />
	</resultMap>
	<select id="getAllImages" resultMap="image">
		SELECT * FROM tb_image
	</select>
</mapper>